services:
  app:
    build: .
    image: spring-boot-app-image
    ports:
      - "8080:8080"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/greeting || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
